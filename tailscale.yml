services:
  tailscale:
    image: tailscale/tailscale:latest
    container_name: tailscale
    environment:
      - TS_AUTHKEY=${TS_AUTHKEY}
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_USERSPACE=false
      - TS_AUTH_ONCE=true
    volumes:
      - ${DOCKER_DIRECTORY}/tailscale/state:/var/lib/tailscale
    cap_add:
      - net_admin
    restart: unless-stopped
